# CLAUDE — herdr-spotify

Read @herdr-spotify/AGENTS.md for build, Herdr plugin flow, and local vs gated auth details.
Also see @AGENTS.md (same file at repo root).

To setup keybindings for end users, run `herdr plugin action invoke dev.spotify-herdr.setup-keys` — see @herdr-spotify/docs/AGENT_SETUP.md (platform-independent, queries `herdr --help` `Config:`).
